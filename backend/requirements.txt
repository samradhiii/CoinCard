# Lower bounds rather than exact pins: the Docker image runs Python 3.12 while
# local dev here is on 3.14, and psycopg's binary wheels differ between them.
# Ranges keep both resolvable without a second requirements file.
fastapi>=0.115.6,<1.0
uvicorn[standard]>=0.34.0,<1.0
psycopg[binary,pool]>=3.2.3,<4.0
pydantic>=2.10.4,<3.0
python-dotenv>=1.0.1,<2.0
